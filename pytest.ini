[pytest]
# The global xonsh pytest plugin starts an interactive Windows console during
# collection, which is unavailable in Unity/CI subprocesses.  Video tests do
# not depend on xonsh, so keep the project command non-interactive and stable.
addopts = -p no:xonsh
