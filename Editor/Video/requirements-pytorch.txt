# Optional quality extras for advanced temporal 3D lifting/refinement. The
# manager installs this file after the base requirements.txt profile.
# PyTorch is intentionally kept in the isolated venv. PyTorch CUDA wheels (cu121)
# are indexed for high-performance GPU extraction on supported NVIDIA hardware.
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.2,<3
torchvision>=0.17,<1
einops>=0.7,<1
tqdm>=4.66,<5
smplx>=0.1.28,<1
